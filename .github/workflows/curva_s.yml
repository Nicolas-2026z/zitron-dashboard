name: Generar Curva S

on:
  schedule:
    - cron: "*/30 11-20 * * 1-5"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  curva_s:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      -
